# Portfolio v2

React + TypeScript + Tailwind + Three.js (react-three-fiber) portfolio site with a scroll-driven 3D avatar and a latte-art gallery page.

See `docs/00-overview.md` for how it's built. Quick start:

```
npm install
npm run dev
```
